'use client';

import CredentialExpirationDashboard from '@/components/admin/credentials/CredentialExpirationDashboard';

export default function CredentialsPage() {
    return <CredentialExpirationDashboard />;
}
